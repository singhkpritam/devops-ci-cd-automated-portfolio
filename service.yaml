apiVersion: v1
kind: Service
metadata:
  name: devops-portfolio-service
spec:
  type: NodePort
  selector:
    app: devops-portfolio
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
